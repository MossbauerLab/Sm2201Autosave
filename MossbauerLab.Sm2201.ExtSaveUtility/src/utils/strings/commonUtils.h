#ifndef SM2201_SPECTRUM_SAVER_SRC_UTILS_STRINGS_COMMON_UTILS_H
#define SM2201_SPECTRUM_SAVER_SRC_UTILS_STRINGS_COMMON_UTILS_H
/* Utils for work with strings in application that are running on Windows 98
 *
 *
 */
#include <string>

/*std::string ltrim(const char* str)
{
    if (str == NULL)
		return std::string();
}*/

//std::string rtrim(const char* str);

#endif